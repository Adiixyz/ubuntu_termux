# ubuntu_termux
Install Ubuntu in Termux

## How to install?
```cmd
$ apt install git
$ apt install bash
$ git clone https://github.com/Adiixyz/ubuntu_termux
$ cd ubuntu_termux
$ bash install.sh
```

## Run...
```cmd
$ bash run.sh
```
